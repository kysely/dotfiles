# Package manifest for install.sh
#
# Format:
#   name [brew=<formula-or-cask-name|->] [apt=<package-name|->] [type=formula|cask] [tap=<homebrew-tap>]
#
# If brew= or apt= is omitted, the package name is used for that manager.
# Use apt=- or brew=- to skip a package for that manager.
# Use type=cask for Homebrew casks.
# Use tap= to tap a Homebrew repository before installing.

bat
btop
build-essential brew=-
caddy brew=-
curl
difftastic apt=-
eza
fd apt=fd-find
fzf
gh
git
jq
lazygit apt=-
lazycaddy apt=- tap=kysely/tap
mosh
neovim
ripgrep
stow
tmux
zoxide
zsh
ghostty type=cask apt=-
